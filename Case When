SELECT MX, MN , PR,
(CASE 
WHEN PR = 'PQ' THEN '전분기'
WHEN PR = 'PM' THEN '전월'
WHEN PR = 'PW' THEN '전주'
WHEN PR = 'D3' THEN '3일전' END) AS '$dfdf'
FROM CALCPT WHERE SUM = 'ANIHNN' AND DIVISION = 'I2' 
ORDER BY WEIGHT DESC LIMIT 2;
